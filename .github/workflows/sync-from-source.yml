name: 🔄 Sync Repositórios

on:
  workflow_dispatch:  # Permite executar manualmente na aba Actions
  schedule:
    - cron: '0 */6 * * *'  # Executa a cada 6 horas (ajuste conforme necessário)

permissions:
  contents: write  # Necessário para o bot fazer push

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout do repositório destino
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 📦 Clonar repositório de origem
        run: git clone --depth 1 https://github.com/HunterTheCraft-OFICIAL/openttd-android-Mobile-Android-.git /tmp/source_repo

      - name: 🔄 Sincronizar arquivos (seguro)
        run: |
          # rsync é mais seguro que cp
          # --exclude='.git' evita sobrescrever o histórico do repositório
          # --exclude='.github/workflows/' evita erro de permissão do GITHUB_TOKEN
          rsync -av --delete \
            --exclude='.git' \
            --exclude='.github/workflows/' \
            /tmp/source_repo/ ./
          rm -rf /tmp/source_repo

      - name: 🔍 Verificar se há alterações
        id: git-check
        run: |
          git add -A
          if git diff --staged --quiet; then
            echo "changes=false" >> $GITHUB_OUTPUT
            echo "✅ Nenhuma alteração detectada."
          else
            echo "changes=true" >> $GITHUB_OUTPUT
            echo "🔄 Alterações detectadas!"
          fi

      - name: 💾 Commit e Push (só roda se houver mudanças)
        if: steps.git-check.outputs.changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit -am "🤖 Auto-sync: atualizado a partir do repositório de origem"
          git push origin ${{ github.ref_name }}
